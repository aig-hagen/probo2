# @package _global_

timeout: 2
name: Test-Multiple-Runs

defaults:
  - override /callbacks:
    - stats/aggregate_results
    - stats/my_stats
    - general/agg_results

hydra:
  sweeper:
    params:
      benchmark: small_i23
      task: SE-PR, SE-CO, SE-ST
      solver: mu-toksia, mu-toksia-v2
      runs: range(1,11)




